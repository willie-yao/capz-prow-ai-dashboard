# Project config for the CAPZ Prow dashboard.
# Consumed by the prow-ai-dashboard engine via .github/workflows/deploy.yml.

id: capz
name: "Cluster API Provider Azure"
short_name: "CAPZ"

# Only jobs that advertise this testgrid dashboard are included.
# Job discovery is fully automatic: the engine asks GitHub code search
# for every YAML under kubernetes/test-infra/config/jobs/ that mentions
# this dashboard, then keeps the jobs whose testgrid-dashboards
# annotation contains it.
testgrid:
  dashboard: "sig-cluster-lifecycle-cluster-api-provider-azure"

# Job grouping. Rules are evaluated in order; first lowercase substring match
# wins. Jobs with no match go in "other".
categories:
  - match: "conformance"
    id: "conformance"
    label: "Conformance"
  - match: "[managed kubernetes]"
    id: "aks-e2e"
    label: "AKS E2E"
  - match: "e2e-aks"
    id: "aks-e2e"
    label: "AKS E2E"
  - match: "capi-e2e"
    id: "capi-e2e"
    label: "CAPI E2E"
  - match: "upgrade"
    id: "upgrade"
    label: "Upgrade"
  - match: "coverage"
    id: "coverage"
    label: "Coverage"
  - match: "scalability"
    id: "scalability"
    label: "Scalability"
  - match: "e2e"
    id: "capz-e2e"
    label: "CAPZ E2E"

# Display order for category sections on the landing page.
category_display_order:
  - capz-e2e
  - aks-e2e
  - upgrade
  - capi-e2e
  - conformance
  - coverage
  - scalability

# GCS bucket holding build artifacts.
gcs:
  bucket: "kubernetes-ci-logs"

# UI branding. Frontend reads these from data/manifest.json at runtime.
branding:
  title: "CAPZ Prow Dashboard"
  base_path: "/capz-prow-ai-dashboard"
  site_url: "https://willie-yao.github.io/capz-prow-ai-dashboard"
  source_repo:
    owner: "kubernetes-sigs"
    name: "cluster-api-provider-azure"

# AI analysis settings. Endpoint and model come from repo variables
# (vars.AI_MODEL / vars.AI_ENDPOINT) wired in .github/workflows/deploy.yml.
ai:
  # Universal agentic path: the LLM browses GCS + cluster artifacts via the
  # filesystem + k8s tools rather than going through a curator step.
  use_universal_path: true

  agentic:
    enabled: true
    always: false
    tools:
      - filesystem
      - k8s
